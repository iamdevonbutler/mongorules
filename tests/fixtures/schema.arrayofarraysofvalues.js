module.exports = {

  "account.locations": [[{
    type: 'string',
    required: true,
    minLength: 1,
    maxLength: 2,
    // transform: function(values) {
    //   return values.map((value) => {
    //     return value.map((val) => {
    //       return val + ' deg';
    //     });
      // });
    // }
  }]],


};
